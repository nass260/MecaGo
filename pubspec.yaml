name: mecago
description: Application premium d'auto-entretien automobile.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1

  # Navigation
  go_router: ^14.2.3

  # State Management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5

  # Injection de dépendances
  get_it: ^8.0.2
  injectable: ^2.5.0

  # Réseau
  dio: ^5.7.0

  # Caméra & OCR
  camera: ^0.11.0+2
  google_mlkit_text_recognition: ^0.13.0
  image: ^4.3.0

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.3
  firebase_messaging: ^15.1.3

  # Stockage local
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.2

  # Permissions
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  injectable_generator: ^2.6.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/
